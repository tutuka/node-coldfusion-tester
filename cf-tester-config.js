'use strict'

const config = {
  host: 'http://localhost:8500',
  basePath: 'API/exchange',
  testFolder: 'test',
  testTypes: ['spec', 'unit'],
  testRunner: 'runner.cfm',
  testFileSuffix: 'Spec'
}

module.exports = config
