'use strict'

const config = {
  host: 'http://localhost:8500',
  basePath: '',
  testFolder: 'test',
  testTypes: ['spec', 'unit'],
  testRunner: 'testbox/system/runners/HTMLRunner.cfm',
  testFileSuffix: 'Spec'
}

module.exports = config
